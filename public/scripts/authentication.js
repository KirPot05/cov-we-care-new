const registerForm = document.getElementById('register_form');

registerForm.addEventListener('submit', async (e) => {
    e.preventDefault();

    console.log(registerForm);

    // const token = await fetch('/users/new', {
    //     method: 'POST', 
    //     body: JSON.stringify({
    //         name: 
    //     })
    // });

})